{
  "name": "mish-academia",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "vite",
    "build": "vite build",
    "images:webp": "node scripts/convert-raster-webp.mjs",
    "lint": "eslint . && stylelint \"css/**/*.css\"",
    "lint:fix": "eslint . --fix && stylelint \"css/**/*.css\" --fix",
    "lint:js": "eslint .",
    "lint:css": "stylelint \"css/**/*.css\"",
    "check": "npm run lint"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "eslint-config-htmlacademy": "^9.0.0",
    "sharp": "^0.34.5",
    "stylelint": "^16.0.0",
    "stylelint-config-htmlacademy": "^4.4.0",
    "vite": "^6.2.0"
  }
}
